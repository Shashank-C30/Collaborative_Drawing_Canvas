{
  "name": "collaborative-drawing-canvas",
  "private": true,
  "scripts": {
    "build": "npm --prefix client ci && npm --prefix server ci && npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server start"
  }
}
